{% extends 'base.html.twig' %}

{% block title %}
  {% if application is not null %}
    Sollictatie {{ application.job.title }} | VAC!T
  {% else %}
    Vacaturesite voor IT | VAC!T
  {% endif %}
{% endblock %}

{% block content %}
  {% if application is not null %}
    <p>
      Bedankt voor je sollicitatie op de vacature voor {{ application.job.title }}
      bij {{ application.job.employer.name }}.
    </p>
    <p>
      Wanneer de werkgever je heeft uitgenodigd voor een gesprek, zal dit zichtbaar
      worden onder 'Mijn Sollicitaties'.
      <br>
      We wensen je veel succes met je sollicitatie!
    </p>
  {% else %}
    <p>
      Je hebt al eerder op deze vacature gesolliciteerd. Bekijk 'Mijn Sollicitaties
      voor een overzicht'.
    </p>
  {% endif %}
{% endblock %}
